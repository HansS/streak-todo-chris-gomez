web:
  build: .
  volumes:
    - .:/streak
  links:
    - es
  ports:
    - "80:9000"
  environment:
    NODE_ENV: development
  command: grunt

es:
  image: dockerfile/elasticsearch
  ports:
    - "9200:9200"