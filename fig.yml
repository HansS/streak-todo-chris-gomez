web:
  build: .
  volumes:
    - .:/streak
  working_dir: /streak
  links:
    - es
  ports:
    - "9000:9000"
  environment:
    NODE_ENV: development
  command: grunt

es:
  image: dockerfile/elasticsearch
  ports:
    - "9200:9200"